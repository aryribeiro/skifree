{
  "name": "skifree",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "verify:logic": "node scripts/verify-logic.mjs",
    "verify": "node scripts/verify-runtime.mjs",
    "build:sites": "vite build && node scripts/prepare-sites-build.mjs"
  },
  "dependencies": {
    "three": "^0.165.0"
  },
  "devDependencies": {
    "playwright-core": "^1.61.1",
    "pngjs": "^7.0.0",
    "vite": "^5.4.19"
  }
}
